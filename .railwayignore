# Was der Prototyp zur Laufzeit nicht liest, muss auch nicht hoch. Ohne diese
# Liste gingen rund 27 MB fertige Folien und PDFs bei jedem Deploy mit.
#
# Gebraucht werden nur: tool/, system-prompt.md, schriften/, aus daten/ die
# Wortliste sowie der Kursplan — und die beiden Abgabe-PDFs, die das
# Dokumente-Modal zum Herunterladen anbietet (zusammen 7,8 MB).

# Mit /* statt / bleibt der Ordner selbst durchlaessig; nur so laesst sich
# eine einzelne Datei darin wieder einschliessen.
ausgabe/*
ausgabe-dokument/*
!ausgabe/Praesentation-KLARTEXT.pdf
!ausgabe-dokument/KLARTEXT-System-Prompt-Dokumentation.pdf

bilder/
entwurf/

*.pptx

# Werkzeuge des Foliensatzes, nicht des Servers
build.py
watch.py
dokument.py
dokument.css
pruefe-design.py
folien.md
dokumentation.md

.DS_Store
__pycache__/

# Die Historie ist 846 MB gross und zur Laufzeit ohne Belang.
.git/
